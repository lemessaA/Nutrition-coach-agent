# Copy to `.env.local` for local dev, or set in Vercel → Project → Settings → Environment Variables.
# Public base URL of your FastAPI backend (no trailing slash).
# Local:        http://localhost:8000
# Production (Vercel → Render): https://<your-api>.onrender.com
# Vercel app example: https://nutrition-coach-agent-kappa.vercel.app
NEXT_PUBLIC_API_URL=http://localhost:8000
